services:
  api:
    build: .
    ports:
      - "8086:8000"
    volumes:
      - ./data:/app/data
      - ${REPOS_DIR:-$HOME/repos}:/repos
    environment:
      - DATA_DIR=/app/data
      - REPOS_DIR=/repos
